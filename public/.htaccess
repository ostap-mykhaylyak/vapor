<IfModule mod_rewrite.c>
    RewriteEngine On
    # Serve direttamente i file esistenti (assets).
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]
    # Tutto il resto al front controller.
    RewriteRule ^ index.php [L]
</IfModule>
